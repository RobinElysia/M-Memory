# 第 3 层 · 集成与端到端测试层 (Integration & E2E)

## 目标
验证多模块串联的长期记忆行为，特别是桶演化、跨桶联想和冲突消解。

## 输入
- 全部核心实现代码
- `ARCHITECTURE_DESIGN.md` 作为行为基准

## Agent 任务
编写并执行 **场景测试**（基于真实对话序列），必须包含以下三个核心场景：

1. **主题漂移与桶分裂**：
   - 输入 10 轮关于“猫”的对话，紧接着 5 轮“狗”的对话，再 1 轮“猫”的对话。
   - 验证：至少形成两个桶，“猫”桶和“狗”桶；猫桶的 Medoid 摘要与猫相关；当回到猫话题时，能正确分配回猫桶；若有跨桶关联边，应合理建立。
2. **矛盾信息与冲突消解**：
   - 对话轮次：用户说“我住在北京”，之后说“我搬到上海了”。
   - 验证：检索时返回的 C 应显示最新置信度最高的信息（上海），旧信息被标记为“过时”且降权，但未删除。
3. **桶休眠与唤醒**：
   - 创建一个桶并写入 3 个节点后，长时间不再写入或查询。
   - 验证：后台任务触发后该桶标记为休眠，其 Medoid 向量移出活跃索引；后续显式查询若匹配该桶主题，桶被唤醒并返回结果。

每个场景测试必须：
- 完全使用**假的嵌入模型**（例如返回固定维度的随机向量，但保证相似度逻辑可控）和**假的 LLM**（按预设规则响应，例如分桶决策、冲突判定），确保测试可重复且快速。
- 断言内容：检索返回的节点列表及顺序、权重分数、桶的 Medoid 变化、后台清理后的节点数量/状态。
- 输出可视化日志（如 ASCII 图或简单的文本图）展示桶、节点、边的状态，Agent 需在报告中解释测试为何通过。

## 验收标准
- 所有场景测试脚本位于 `tests/e2e/`，运行 `pytest tests/e2e/` 全绿。
- Agent 生成 `E2E_REPORT.md`，包含每个场景的简述、预期行为、实际结果、可视化图表及通过结论。

## 交付物
- `tests/e2e/` 目录及测试脚本
- `E2E_REPORT.md`

通过后，颁发 **Layer 3 Pass Token**，进入第 4 层。