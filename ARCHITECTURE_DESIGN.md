# 记忆系统架构设计（权威描述）

*以下为最终确认的系统设计，任何实现必须严格参照此文档，不可偏离。*

我们将每轮对话按时间顺序存储，并对每轮对话用大模型抽象出一个总结性段落/关键词（A）和待存详细内容（C）。将 A 和 C 分别向量化，A 的向量用于第一层粗筛，C 的向量用于第二层精确检索。

第一层采用“增量聚类+LLM决策+图关联”的机制进行组织，不再使用随机桶，而是维护一组动态桶，每个桶有一个实际节点作为其代表（Medoid），并携带Medoid的摘要文本和向量。当新对话产生 A 时，先计算 A 向量与所有桶 Medoid 向量的相似度，筛选出相似度最高的 top_k（如k=2或3）个候选桶。之后，将当前 A 的文本、这些候选桶的 Medoid 摘要文本（以及桶内离 Medoid 最近的少数几个节点的摘要，作为上下文）发送给大模型，由大模型做出决策：归入某个候选桶、归入多个候选桶（软分配）或创建一个新桶。无论大模型如何决策，我们只将 A 节点物理存储于一个主桶（通常是得分最高的桶或新桶），而不进行多桶的物理冗余复制。若大模型判定 A 与某些非主桶也高度相关，我们不复制节点，而是直接建立跨桶关联边，但为每个节点设定跨桶出度上限与权重衰减阈值，防止边数量指数膨胀；当出度已满且有更高权重的新连接时，采用末位淘汰，比较并替换最弱边。图结构由此自然形成：节点就是各轮对话的 A，边包括按时间顺序连接的时间边、连接节点与其主桶 Medoid 的桶内边，以及前面建立的跨桶关联边。跨桶边的权重可由大模型给出的相关程度或向量相似度决定。该图是增量维护的，每当节点加入主桶后，仅重新计算该桶的 Medoid（以及极少数与之有跨桶边的桶的 Medoid，若必要），且 Medoid 以桶元数据指针形式存在，漂移时同步更新桶摘要与版本号，无需重建桶内边；关联桶的 Medoid 若受影响，则异步标记待刷新，避免阻塞写入路径。后续所有桶级检索，均使用这些 Medoid 的向量进行。

第一层语义搜索时，我们将查询向量化，先与所有桶的 Medoid 向量匹配，找到语义最相近的 top_m 个桶。在每个桶内部，再用查询向量快速检索 top_p 个最相似的 A 节点。然后，利用图结构进行联想扩展：从这些种子节点出发，沿着跨桶边游走（可通过设置“跨桶程度”参数，如最大跳数、边权重阈值来控制），收集关联节点，去重后按最优路径的累积权重排序并全局截断，若同一节点经多条路径可达，取置信度最高的单一路径而非简单累加，避免环路导致的分数膨胀，最终合并成一个有序的候选 A 列表。

进入第二层后，我们根据候选 A 列表取出对应的详细内容 C。为了解决时序和矛盾问题，我们在此执行一个轻量级的冲突消解管线：先将所有候选 C 按“时间新近度 + 与查询的语义相似度 + 来源置信度”进行加权重排序（例如 score = α·sim(C,query) + β/(1+ΔT) + γ·confidence），并截断至 TOP-N 以控制上下文长度。同时，可调用一次大模型，让它专门检查最新信息是否与前几次对话中存储的旧信息构成事实冲突；若有，则采纳最新、置信度最高的表述，并标记旧信息为“过时”，但旧信息不移除，仅降权进入撤回候选集，供深层回溯时引用。对于旧数据的清理，我们不再每条对话都执行，而是设定一个周期性后台任务：扫描各桶内的 C，如果发现两个 C 语义极度相似但关键信息矛盾，且新信息的置信度更高，则自动更新或软删除旧数据；如果某个 C 与其所在桶的 Medoid 相似度长期低于阈值，说明话题漂移，可将其移入冷存储或直接删除。此外，当某桶节点数过大且出现明显子簇时，可触发桶分裂；长期无新增且无查询命中的桶则标记为休眠，其 Medoid 向量暂移出活跃索引，仅在深度检索或显式唤醒时重新加载，以降低维护与检索开销。

这样，通过“刚性分桶+柔性图边”的混合，我们既获得了桶的检索加速，又避免了硬聚类的边界局限，且没有物理冗余，整个系统在成本、准确率和响应速度上更容易取得平衡。