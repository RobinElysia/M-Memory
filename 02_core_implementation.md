# 第 2 层 · 核心实现层 (Core Implementation)

## 目标
以测试驱动的方式实现所有模块，保证逻辑正确性、确定性、可观测性。

## 输入
- 第 1 层的接口、模型、项目骨架

## Agent 工作单元与流程
按模块拆分，逐个攻克：`bucket_manager.py`, `graph_engine.py`, `retrieval.py`, `cleanup.py`, `llm_decision.py` 等。
**每个模块必须遵循以下步骤：**

1. **先写测试**：Agent 生成对应的 `test_module.py`，覆盖：
   - 正常流程（happy path）
   - 边界情况（空桶、单节点、重复节点）
   - 错误路径（无效输入、LLM 调用失败模拟）
2. **后写实现**：Agent 编写模块代码，目标使测试全部通过。
3. **自修复循环**：运行 `pytest`，若失败，Agent 自行修复（最多 3 次迭代），超过则报警并请求人工干预。

## 特殊实现约束
- **LLM 调用必须可观测**：涉及 LLM 的地方需使用结构化日志（`logging` 或 `structlog`），记录 prompt 长度、token 消耗（模拟值）、决策结果。
- **图操作原子性**：节点/边创建、Medoid 更新必须封装在事务中（如果底层存储不支持事务，需实现异常回滚逻辑）。
- **向量存储可替换**：默认使用内存 FAISS 或 NumPy，但必须严格遵守 `VectorStore` 接口，便于后续切换。
- **绝对确定性和无随机**：任何地方不得使用 `random`，Medoid 计算必须确定性（如选择平均距离最小的实际节点），分桶决策必须由 LLM 做出，不得引入随机选择。
- **物理单桶存放**：严格确保每个 A 节点只存储在一个主桶中，跨桶边仅作为关联，不得复制节点。
- **后台清理**：周期性任务使用 `asyncio` 或线程实现，不阻塞主写入路径，需有启动/停止控制。

## 审查检查项 (Agent 自审)
- 所有随机因素已消除。
- A 节点未物理复制。
- 图游走去重采用最优单路径（置信度最高），非累加。
- 冲突消解管线已正确实现重排序与“过时”标记。
- `config.py` 中的所有参数均被使用且可配置。

## 验收标准
- 运行 `pytest tests/`，所有单元测试通过，覆盖率 > 85%。
- 所有模块通过 `mypy` 严格类型检查。
- 运行 `ruff check` 或 `black` 格式检查无报错。
- 能够运行一个简单的集成脚本（手工触发写入和检索），输出符合日志规范。

## 交付物
- `memory_system/` 下所有实现模块
- `tests/` 下单元测试文件
- 测试通过报告（Agent 生成）

通过后，颁发 **Layer 2 Pass Token**，进入第 3 层。